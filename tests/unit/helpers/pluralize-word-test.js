import {
  pluralizeWord
} from 'klarna-techtest/helpers/pluralize-word';

module('PluralizeWordHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = pluralizeWord(42);
  ok(result);
});
